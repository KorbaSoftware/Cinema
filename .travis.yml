before_install:
  - chmod +x gradlew

notifications:
    slack: korbasoftware:cX8FwzEugFfCbagL2EfgZEjw

dist: trusty

language: java
jdk:
  - oraclejdk8


addons:
  sonarcloud:
    organization: korbasoftware # the key of the org you chose at step #3
    branches :
        - fryc-sonarIntegration
    token :
        - 862646087ffb0752a2db8d1fac16297e0c3b367a


script:
  # other script steps might be done before running the actual analysis
  - ./gradlew sonarqube
  - sonar-scanner -Dsonar.login=$SONAR_TOKEN -h -X
  - gradle check

